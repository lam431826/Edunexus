package com.edunexus.domain.enums;

/** H1 = single course purchase, H2 = class enrollment, H3 = course-group subscription. */
public enum EnrollmentAccessType {
    H1_COURSE,
    H2_CLASS,
    H3_GROUP_SUBSCRIPTION
}
